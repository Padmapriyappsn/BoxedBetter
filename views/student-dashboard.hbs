<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Dashboard</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1 style="color: white;">Student Dashboard</h1>
        <nav style="display: flex; align-items: center;">
            <label for="nav-dropdown" style="margin: 0; padding: 5px 10px;">Menu</label>
            <select id="nav-dropdown" onchange="window.location.href=this.value;" style="margin: 0; padding: 5px 10px;">
                <option value="#">Select an option</option>
                <option value="/student/view-order">View Order</option>
                <option value="/student/place-order">Place Order</option>
                <option value="/track-order">Track Order</option>
            </select>
            <a href="/student/logout" style="color: black; text-decoration: none; margin: 0; padding: 5px 10px;">Logout</a>
        </nav>
    </header>

    <section>
        <h2>Available Food Items</h2>

        {{#if foods.length}}
            <ul class="food-list">
                {{#each foods}}
                    <li class="food-item">
                        <h3>{{this.foodName}}</h3>
                        <p><strong>Restaurant:</strong> {{this.restaurantName}}</p>
                        <p><strong>Description:</strong> {{this.description}}</p>
                        <p><strong>Discounted Price:</strong> ${{this.discountedPrice}}</p>
                        <form action="/student/order/{{this.foodId}}" method="POST">
                            <button type="submit">Order Now</button>
                        </form>
                    </li>
                {{/each}}
            </ul>
        {{else}}
            <p>No food items available at the moment. Please check back later!</p>
        {{/if}}
    </section>
</body>
</html>
