<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Restaurant Orders</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>Restaurant Dashboard</h1>
        <nav>
            <label for="nav-dropdown">Menu</label>
            <select id="nav-dropdown" onchange="window.location.href=this.value;">
                <option value="#">Select an option</option>
                <option value="/restaurant/orders">View Orders</option>
                <option value="/post-new-item">Post New Item</option>
            </select>
            <a href="/restaurant/logout" style="color: black; text-decoration: none">Logout</a>
        </nav>
    </header>
    <h1>Restaurant Orders</h1>

    {{#if orders.length}}
        <ul>
            {{#each orders}}
                <li>
                    <strong>Order ID:</strong> {{this.orderId}}<br>
                    <strong>Food Name:</strong> {{this.foodName}}<br>
                    <strong>Quantity:</strong> {{this.quantity}}<br>
                    <strong>Total Price:</strong> ${{this.totalPrice}}<br>
                    </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No orders found.</p>
    {{/if}}

    <a href="/restaurant-dashboard">Back to Dashboard</a>
</body>
</html>